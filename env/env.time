# The chain time options that should be appended if the --competition option is used.
# How long in real time each block should be mined
BLOCK_TIME=12
# How much time to advance per mined block
BLOCK_TIMESTAMP_INTERVAL=624
